# `qmd_py.config`

Application settings, loaded from `MARQ_*` environment variables or a
`.env` file — see [Configuration](../configuration.md) for the
user-facing reference.

::: qmd_py.config
    options:
      filters: ["!^__"]
