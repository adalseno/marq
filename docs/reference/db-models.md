# `qmd_py.db.models`

The `SQLModel` table definitions: `User`, `Collection`, `CollectionGrant`,
`Content`, `Document`, `LlmCache`, `EmbeddingModel`, and their
relationships — the schema [Architecture › Storage](../architecture.md#storage)
describes.

::: qmd_py.db.models
    options:
      filters: ["!^__"]
