# `qmd_py.search.vector`

Vector search: pgvector embeddings, one physical table per embedding
model, chunking, and the HNSW candidate-CTE query.

::: qmd_py.search.vector
    options:
      filters: ["!^__"]
